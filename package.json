{
  "name": "bt",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "commit": "git-cz",
    "build": "pnpm run -C packages/bt-cli build",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "lint": "pnpm run -C packages/bt-cli lint:eslint && pnpm run -C packages/bt-cli lint:prettier",
    "release": "changesets publish",
    "version-packages": "changeset version"
  },
  "config": {
    "commitizen": {
      "path": "node_modules/cz-git",
      "useEmoji": true
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.28.1",
    "cz-git": "^1.11.0"
  }
}
